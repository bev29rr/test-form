# test-form
Building a forum with Laravel (Laracast tutorial)

## Seeding database
```sh
php artisan migrate:fresh --seed
```

NOTE: An ``` admin ``` user with password ``` admin123 ``` has been added